{
  "name": "hermes-mobile",
  "private": true,
  "version": "0.1.0",
  "description": "Hermes Web — the Hermes Desktop chat UI as a browser app. Contains only the web code + nix flake; hermes-agent renderer sources are fetched by nix at build/dev time.",
  "scripts": {
    "dev": "pnpm --filter web-desktop run dev",
    "build": "pnpm --filter web-desktop run build",
    "typecheck": "pnpm --filter web-desktop run typecheck",
    "deploy": "bash apps/web-desktop/scripts/deploy.sh"
  }
}
